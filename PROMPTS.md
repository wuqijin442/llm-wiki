# 🗣 操作提示词手册

> 复制即用。所有提示词都以 `AGENTS.md` 为最高规则，Agent 执行前必须读取它。
> 工作目录固定为本 Vault 根目录（`llmWiki/`）。

## 快捷指令速查

| 你说 | Agent 做什么 |
| --- | --- |
| `ingest` | 消化 `raw/` 中的新素材（O-P-A-R 协议）→ 摘要 / 实体 / 概念 / 双链 / 索引 / 日志 / churn |
| `query <问题>` | 只基于已编译的 `wiki/` 回答，附引用与来源路径 |
| `lint` | 健康检查：矛盾 / 孤岛 / 断链 / 缺字段 / 过时 / 未消化素材；比较前可靠性 |
| `publish <主题> 为 <类型>` | 产出成品到 `output/`（双链转标准链接） |
| `refresh <主题>` | 联网重校验过时内容，旧结论标 `superseded` |
| `生长复盘` | 核对 `growth.md` 指标 + churn/travel + `lifecycle.md` 状态 |
| `disposition 实验` | 按 §8 登记可移除习惯注入，到期结晶判读 |

---

## 1. 单篇 ingest（最常用）

```text
请读取 raw/articles/2026-09-12-xxx.md，严格按照 AGENTS.md 的规则执行增量 ingest。
协议：observe → propose → act → reflect（AGENTS.md §7）。
1. observe：差异检测 + 读素材全文 + 扫 growth/lifecycle 相关指标；
2. propose：先给出拟建/拟补页面清单与双链计划，等我确认再 act；
3. act：摘要页 + 增量归并（优先补已有页）+ 双链（新页 ≥2 出链、≥1 入链）+ 冲突按 §5 不抹平；
4. reflect：更新 index/log；growth 复盘表记 added/revised/merged 与 churn；写下本 episode 的意外与待核实。
完成汇报：新建页、补充页、churn、待核实。
```

## 2. 批量 ingest（消化积压素材）

```text
请读取 raw/ 中新增的素材，并严格按照 AGENTS.md 的规则增量维护 Wiki。

处理方式：
1. 先用「未被任何 wiki 页 sources 引用」的条件筛出待消化素材，列清单给我确认；
2. 按素材之间的相关性分批（同一主题的一起消化，便于复用实体页/概念页）；
3. 逐批执行 ingest，每批结束后汇报新增/更新页面；
4. 全部完成后，核对 wiki/growth.md 的「未消化素材」是否归零。
```

## 3. query（带引用与缺口标注）

```text
请基于现有 Wiki 回答三个问题：
① <问题一>？
② <问题二>？
③ <问题三>？

要求：
- 先从 wiki/ 定位相关页面，再沿双链回到 raw 原文交叉验证；
- 回答时标注引用的 Wiki 页面与 Raw 原文路径；
- 如果资料中存在不同观点、信息缺口或待核实内容，单独列一节说明，不要用推测填补。
```

> 通用的"深度理解"三问（来自 LLM Wiki 实践）：
> **① 这批资料的核心观点是什么？② 涉及的概念、实体、主题之间是什么关系？③ 哪些结论可以转化为可执行的动作？**

## 4. lint（健康检查）

```text
请对本知识库执行一次 lint 健康检查，按 AGENTS.md §7 的检查项逐条过一遍：
矛盾 / 孤立页面 / 缺失概念页 / 过时声明 / 断链与失效 sources / 未消化素材 / 字段完整性 / 生命周期。

若我要与上期比较生长：
0. 先做「比较前可靠性」抽查——对同一固定输入集重跑检查项，结论不一致则本次比较作废并说明；
1. 问题清单（按严重度排序），每条给出涉及页面与证据；
2. 修复建议，区分「可直接修」和「需我裁决」；
3. 先别改文件，等我确认后再执行修复，并记录到 wiki/log.md。
```

## 5. publish（产出成品）

```text
请以「<主题>」为题目，写一篇 <类型：post|report|slides|tutorial|newsletter>，受众是 <受众>。
要求：
1. 从 wiki/index.md 定位相关页面并阅读来源；
2. 草稿输出到 output/<类型目录>/<文件名>.md；
3. 成品必须独立可读——把 [[双链]] 转成标准 Markdown 链接；
4. 完成后记录到 wiki/log.md，并告诉我引用了哪些页面。
```

## 6. refresh（联网重校验）

```text
请对「<主题>」做一次 refresh：
1. 先读现有 wiki 页面，列出其中的时效性声明（版本号、价格、能力、排名等）；
2. 联网检索最新信息，逐条对比，明确指出"变了 / 没变 / 查不到"；
3. 差异给我确认后再更新 wiki：旧结论标 status: superseded 并加 superseded_by，不要直接删；
4. 新信息另存为 raw 素材（走正规 ingest 流程），并更新 lifecycle.md 的复核记录。
```

## 7. 冲突裁决（发现两种说法时）

```text
这两处描述冲突了：[[页面A]] 说 <说法一>，[[页面B]] 说 <说法二>。
请按 AGENTS.md §5 处理：
1. 分别追溯各自的来源与时间；
2. 判断是"表述差异"、"适用范围不同"还是"真冲突"；
3. 若判定为真冲突，保留双方并标注 scope 与 confidence，不要合成一句折中结论；
4. 改动前先给我看方案。
```

## 8. 生长复盘（每周）

```text
请做一次生长复盘：
1. 读 wiki/growth.md 的四项指标：未消化素材 / 零出链页 / 缺字段页 / 超 90 天未更新；
2. 读变动分解：复盘表的 added/revised/merged 与 churn；估算累计 travel；若连续多期 churn≈0，建议一次熵减 lint；
3. 读 wiki/lifecycle.md 的状态分布与 seed 页清单；
4. 若需要与历史比较：先按 AGENTS.md §7「比较前可靠性」抽查口径自复现，不一致则不下比较结论；
5. 给出本周最该做的三件事（按性价比排序）；
6. 把复盘结论追加到 growth.md §生长复盘表 与 wiki/log.md。
```

## 9. disposition 实验（可移除习惯注入）

```text
请按 AGENTS.md §8 启动一次 disposition 实验：
- 注入内容（单一差异）：<一句话习惯/要求>
- 载体：<PROMPTS 片段 | 临时 checklist | 会话提示>
- 期限：<N 次同类操作>
- 预期可观测行为：<可检查的行为>

要求：
1. 先在 wiki/log.md 登记实验（label 可用 exp-<短语>）；
2. 期限内每次相关操作附带注入并在 log 记遵守情况；
3. 到期提醒我撤除注入（不留残句）；
4. 随后 2–3 次操作做结晶判读：行为仍在 → 提案升格为正式 Schema；消失 → 不升格。
```

---

## 收集端提示词（素材怎么进来）

| 场景 | 做法 |
| --- | --- |
| 电脑看文章 | Obsidian Web Clipper → 一键剪藏到 `raw/articles/`（命名为 `YYYY-MM-DD-标题`） |
| 手机上刷到好内容 | ima 知识管家 / WorkBuddy 小程序 → 落到 `raw/00-Inbox/` |
| 聊天记录、会议纪要 | 导出为 Markdown 放 `raw/10-Work/`，或直接对我说"把这段对话归档进 raw" |
| 临时冒出的想法 | 写进 `raw/01-Daily/` 的每日笔记「想法收集」，攒够一批再 ingest |

> 私人资料是否同步、同步到哪，由你自己决定——Obsidian 是本地文件夹，数据自主。

---

## 心法

1. **不要等准备好才开始**：先建结构 → 放一篇真实素材 → 让 Agent 完成第一次增量更新 → 再按实际体验调 Schema。
2. **看结果不看流程**：一次 ingest 结束后，`wiki/` 必须真的有变化；没变化就是失败。
3. **人是判断层，Agent 是维护层**：哪些资料值得留、规则怎么定、结论成不成立——你定；重复的结构化维护——它做。
