---
title: 意图路由与服务端兜底
aliases:
  - 智能体意图路由
  - enforceCodeChangeCard
tags:
  - AI智能体
  - 模式
  - 鲁棒性
  - 概念
category: concepts
created: 2026-09-12
updated: 2026-09-12
sources:
  - "[[raw/20-Tech/记忆-DGX-AI智能体工程铁律]]"
description: 用正则+粘性把请求路由到 agent 能力，并在 LLM 自发失败时以服务端确定性兜底，确保关键意图不丢失。
status: seed
---

# 意图路由与服务端兜底

## 概述

智能体需要判断"这条请求要不要升级为 agent 能力调用"。纯靠 LLM 自觉地升是不可靠的，因此用**确定性路由 + 服务端兜底**保证关键意图（尤其是改码）不被吞掉。

## 意图路由

- **正则路由**：以"实体 × 动作 + 负向断言"正则升级为 agent；能力/健康问句、改码/改界面请求各配独立正则。
- **粘性命中（stickyHit）**：短时 Map（如 8min），**双键**写入（会话键 + 账号键），任一命中即粘：
  - 账号键命中要求问句含实体词（防知识问句被拽进 agent）；
  - 会话键无条件粘（前端首轮常不带 session_id，二轮才带）。
- **两端点族陷阱**：RAG 端点族走 tryTaskIntent，命中不了正则就**掉纯 RAG（无工具）**。任何新意图都要同时测两个端点族，别只测直连 agent 端点。

## 服务端确定性兜底

- **问题**：LLM 遇"修改界面样式"类请求可能**先反问细节而不登记卡片**（实测多次）。
- **方案**：agent 主循环出口加强制补卡（如 `enforceCodeChangeCard`）——命中改码意图且本轮无 `create_change_request` → 服务端**强制补登记**一张审批卡片（risk=medium，描述标"【系统强制登记】待补充"）。
- 零副作用：批准前不执行。

## 相关
- [[AI智能体分权治理]] - 兜底卡进入审批流程
- [[自愈闭环设计]] - 兜底卡由批准执行器消费
- [[AI智能体工程方法论]] - 综合方法论